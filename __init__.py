# from os.path import dirname, basename, isfile, join
# import glob

# module_config_research = '/Users/alvarosf07/Projects/MyProjects/Quant-RDE-SAT/config/config-research/default_parameters.py'
# module_APIs = '/Users/alvarosf07/Projects/MyProjects/Quant-RDE-SAT/lib/lib-data/APIs/APIBaseClass.py'
# #sys.path.append()
# #sys.path.append(module_APIs)
# modules = (glob.glob(join(dirname(module_config_research), "*.py"))
#            +glob.glob(join(dirname(module_APIs), "*.py"))
#            +glob.glob(join(dirname(__file__), "*.py")))
# __all__ = [ basename(f)[:-3] for f in modules if isfile(f) and not f.endswith('__init__.py')]


# 1) Configure APIs for some important pages
# 2) Create clean and consistent API structure that allows to use my personal api syntax to call any api from any page
# 3) Complete AssetClass and main methods
# 4) Complete PortfolioClass
# 5) Complete ...
